@using W3Css.Blazor.Internal
@inherits W3ComponentBase

<span @attributes="AdditionalAttributes"
      class="@CssClass"
      style="@StyleValue"
      role="@RoleValue"
      aria-label="@Label">
    @if (!string.IsNullOrWhiteSpace(Src))
    {
        <img src="@Src"
             alt="@Alt"
             loading="@Loading" />
    }
    else if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        <span class="w3-avatar-initials">@Initials</span>
    }
</span>

@code {
    private string CssClass => BuildClass(
        "w3-avatar",
        Circle ? "w3-circle" : Round.ToClass(),
        Border ? "w3-border" : null,
        Color.ToBackgroundClass(),
        TextColor.ToTextClass());

    private string? StyleValue => CombineStyle(Size > 0 ? $"--w3-avatar-size:{Size}px" : null, Style);

    private string? RoleValue => string.IsNullOrWhiteSpace(Label) ? null : "img";

    /// <summary>
    /// Gets or sets the content rendered inside the component.
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    /// <summary>
    /// Gets or sets the src value.
    /// </summary>
    [Parameter]
    public string? Src { get; set; }

    /// <summary>
    /// Gets or sets the alt value.
    /// </summary>
    [Parameter]
    public string Alt { get; set; } = string.Empty;

    /// <summary>
    /// Gets or sets the initials value.
    /// </summary>
    [Parameter]
    public string? Initials { get; set; }

    /// <summary>
    /// Gets or sets the accessible label text.
    /// </summary>
    [Parameter]
    public string? Label { get; set; }

    /// <summary>
    /// Gets or sets the loading value.
    /// </summary>
    [Parameter]
    public string Loading { get; set; } = "lazy";

    /// <summary>
    /// Gets or sets the size value.
    /// </summary>
    [Parameter]
    public int Size { get; set; } = 40;

    /// <summary>
    /// Gets or sets the circle value.
    /// </summary>
    [Parameter]
    public bool Circle { get; set; } = true;

    /// <summary>
    /// Gets or sets the border value.
    /// </summary>
    [Parameter]
    public bool Border { get; set; }

    /// <summary>
    /// Gets or sets the round value.
    /// </summary>
    [Parameter]
    public W3Round Round { get; set; } = W3Round.Medium;

    /// <summary>
    /// Gets or sets the background color class.
    /// </summary>
    [Parameter]
    public W3Color Color { get; set; } = W3Color.LightGrey;

    /// <summary>
    /// Gets or sets the text color class.
    /// </summary>
    [Parameter]
    public W3Color TextColor { get; set; } = W3Color.Black;

    private static string? CombineStyle(params string?[] styles)
    {
        var style = string.Join(';', styles
            .Where(value => !string.IsNullOrWhiteSpace(value))
            .Select(value => value!.Trim().TrimEnd(';')));

        return string.IsNullOrWhiteSpace(style) ? null : style;
    }
}
