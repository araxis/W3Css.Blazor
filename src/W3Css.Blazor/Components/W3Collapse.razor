@using W3Css.Blazor.Internal
@inherits W3ComponentBase

<div @attributes="AdditionalAttributes"
     class="@CollapseClass"
     aria-expanded="@AriaExpanded"
     aria-hidden="@AriaHidden"
     style="@Style">
    @if (IsExpandedResolved)
    {
        @ChildContent
    }
</div>

@code {
    private string CollapseClass => BuildClass(
        "w3-collapse-content",
        IsExpandedResolved ? "w3-show" : "w3-hide",
        Animate ? "w3-animate-opacity" : null,
        Border ? "w3-border" : null,
        Round.ToClass(),
        Color.ToBackgroundClass(),
        TextColor.ToTextClass());

    private bool IsExpandedResolved => IsExpanded ?? Expanded;

    private string AriaExpanded => IsExpandedResolved ? "true" : "false";

    private string AriaHidden => IsExpandedResolved ? "false" : "true";

    /// <summary>
    /// Gets or sets the expanded value.
    /// </summary>
    [Parameter]
    public bool Expanded { get; set; }

    /// <summary>
    /// Gets or sets the callback raised when the expanded value changes.
    /// </summary>
    [Parameter]
    public EventCallback<bool> ExpandedChanged { get; set; }

    /// <summary>
    /// Gets or sets the is expanded value.
    /// </summary>
    [Parameter]
    public bool? IsExpanded { get; set; }

    /// <summary>
    /// Gets or sets the callback raised when the is expanded value changes.
    /// </summary>
    [Parameter]
    public EventCallback<bool> IsExpandedChanged { get; set; }

    /// <summary>
    /// Gets or sets the animate value.
    /// </summary>
    [Parameter]
    public bool Animate { get; set; } = true;

    /// <summary>
    /// Gets or sets the border value.
    /// </summary>
    [Parameter]
    public bool Border { get; set; }

    /// <summary>
    /// Gets or sets the round value.
    /// </summary>
    [Parameter]
    public W3Round Round { get; set; } = W3Round.None;

    /// <summary>
    /// Gets or sets the background color class.
    /// </summary>
    [Parameter]
    public W3Color Color { get; set; } = W3Color.None;

    /// <summary>
    /// Gets or sets the text color class.
    /// </summary>
    [Parameter]
    public W3Color TextColor { get; set; } = W3Color.None;

    /// <summary>
    /// Gets or sets the content rendered inside the component.
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    /// <summary>
    /// Sets the expanded state and raises the matching changed callback.
    /// </summary>
    public async Task SetExpandedAsync(bool expanded)
    {
        if (IsExpanded.HasValue)
        {
            await IsExpandedChanged.InvokeAsync(expanded);
            return;
        }

        if (Expanded != expanded)
        {
            Expanded = expanded;
            await ExpandedChanged.InvokeAsync(expanded);
        }
    }

    /// <summary>
    /// Toggles the expanded state.
    /// </summary>
    public Task ToggleAsync()
    {
        return SetExpandedAsync(!IsExpandedResolved);
    }
}
