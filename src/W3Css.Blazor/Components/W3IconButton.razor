@using Microsoft.AspNetCore.Components.Web
@using W3Css.Blazor.Internal
@inherits W3ComponentBase

<button @attributes="AdditionalAttributes"
        type="@Type"
        class="@CssClass"
        style="@Style"
        aria-label="@EffectiveLabel"
        aria-pressed="@AriaPressed"
        title="@Title"
        disabled="@Disabled"
        @onclick="HandleClickAsync">
    <W3Icon IconClass="@IconClass" Class="@IconCssClass">@ChildContent</W3Icon>
</button>

@code {
    private string CssClass => BuildClass(
        "w3-button",
        "w3-icon-button",
        Circle ? "w3-icon-button-circle w3-circle" : null,
        Border ? "w3-border" : null,
        Toggle && Pressed ? "w3-icon-button-pressed" : null,
        Color.ToBackgroundClass(),
        TextColor.ToTextClass(),
        Size.ToClass(),
        Round.ToClass(),
        Disabled ? "w3-disabled" : null);

    private string IconCssClass => new W3ClassBuilder()
        .Add("w3-icon-button-icon")
        .Add(IconElementClass)
        .ToString();

    private string EffectiveLabel => string.IsNullOrWhiteSpace(Label) ? "Icon action" : Label;

    private string? AriaPressed => Toggle ? Pressed.ToString().ToLowerInvariant() : null;

    [Parameter]
    public string Label { get; set; } = "Icon action";

    [Parameter]
    public string? Title { get; set; }

    [Parameter]
    public string? IconClass { get; set; }

    [Parameter]
    public string? IconElementClass { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [Parameter]
    public W3Color Color { get; set; } = W3Color.None;

    [Parameter]
    public W3Color TextColor { get; set; } = W3Color.None;

    [Parameter]
    public W3Size Size { get; set; } = W3Size.Default;

    [Parameter]
    public W3Round Round { get; set; } = W3Round.Medium;

    [Parameter]
    public bool Border { get; set; }

    [Parameter]
    public bool Circle { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

    [Parameter]
    public bool Toggle { get; set; }

    [Parameter]
    public bool Pressed { get; set; }

    [Parameter]
    public string Type { get; set; } = "button";

    [Parameter]
    public EventCallback<MouseEventArgs> OnClick { get; set; }

    private Task HandleClickAsync(MouseEventArgs args)
    {
        return Disabled ? Task.CompletedTask : OnClick.InvokeAsync(args);
    }
}
